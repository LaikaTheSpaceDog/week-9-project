import React from 'react';

const squareStyle = {
    backgroundColor: "red",
    width: 200,
    height: 200
};

const Square = () => (
    <div style={squareStyle}>

    </div>
);

export default Square;